name: terraform
description: Template para você iniciar a prover sua infraestrutura com código através do Terraform
types:
  - env-template
inputs:
    - label: Provider version
      type: text
      name: provider_version
      default: "2.0"
    - label: Provider region
      type: text
      name: provider_region
      default: "us-east-1"
